node_modules
.next
out
build
# Every .env variant except the committed placeholder file. `.env` and
# `.env*.local` alone left `.env.production` and `.env.development` tracked —
# exactly the two names you reach for when copying real values out of Vercel.
.env
.env.*
!.env.example
dev.db
dev.db-journal
prisma/dev.db
prisma/dev.db-journal
*.log
.DS_Store
next-env.d.ts

# Local Claude Code settings (keep out of version control)
.claude/settings.local.json

# Sentry
.sentryclirc

# Secrets & personal files — never commit
Passwords.txt
*.tsbuildinfo

# MCP config — contains a Supabase access token, never commit
.mcp.json

# Python bytecode from local runner syntax checks
__pycache__/
*.pyc
